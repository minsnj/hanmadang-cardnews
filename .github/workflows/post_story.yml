name: 한마당 스토리 포스팅

on:
  workflow_dispatch:
    inputs:
      link_url:
        description: '포스트 링크 URL'
        required: true

jobs:
  post-story:
    runs-on: ubuntu-latest
    steps:
      - name: Instagram 스토리 포스팅
        env:
          INSTAGRAM_ACCESS_TOKEN: ${{ secrets.INSTAGRAM_ACCESS_TOKEN }}
          INSTAGRAM_USER_ID: ${{ secrets.INSTAGRAM_USER_ID }}
          LINK_URL: ${{ github.event.inputs.link_url }}
        run: |
          python3 - << 'PYEOF'
          import urllib.request, urllib.parse, urllib.error, json, time, os
          TOKEN     = os.environ["INSTAGRAM_ACCESS_TOKEN"]
          USER_ID   = os.environ["INSTAGRAM_USER_ID"]
          LINK_URL  = os.environ["LINK_URL"]
          IMAGE_URL = "https://github.com/minsnj/hanmadang-cardnews/releases/download/hamadang-apps-v1/story.png"

          def ig_post(path, data):
              url  = f"https://graph.instagram.com/v21.0{path}"
              data["access_token"] = TOKEN
              body = urllib.parse.urlencode(data).encode()
              req  = urllib.request.Request(url, data=body, method="POST")
              try:
                  with urllib.request.urlopen(req) as r:
                      return json.loads(r.read())
              except urllib.error.HTTPError as e:
                  raise RuntimeError(f"오류 ({e.code}): {e.read().decode()}")

          def ig_get(path):
              url = f"https://graph.instagram.com/v21.0{path}&access_token={TOKEN}"
              with urllib.request.urlopen(url) as r:
                  return json.loads(r.read())

          print(f"링크: {LINK_URL}")
          print(f"이미지: {IMAGE_URL}")

          # 컨테이너 생성
          res = ig_post(f"/{USER_ID}/media", {
              "image_url":  IMAGE_URL,
              "media_type": "STORIES",
              "url":        LINK_URL,
          })
          container_id = res["id"]
          print(f"컨테이너 ID: {container_id}")
          print(f"컨테이너 응답 전체: {json.dumps(res)}")

          # 컨테이너 상태 확인
          time.sleep(5)
          status = ig_get(f"/{container_id}?fields=id,status_code,status,url")
          print(f"컨테이너 상태: {json.dumps(status)}")

          # 게시
          result = ig_post(f"/{USER_ID}/media_publish", {"creation_id": container_id})
          print(f"✅ 스토리 게시 완료! ID: {result['id']}")
          PYEOF
